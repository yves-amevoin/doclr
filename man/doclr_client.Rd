% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{doclr_client}
\alias{doclr_client}
\title{Create a connection to a docling-serve server}
\usage{
doclr_client(
  url = Sys.getenv("DOCLING_SERVE_URL", DOCLR_DEFAULT_URL),
  api_key = Sys.getenv("DOCLING_SERVE_API_KEY"),
  timeout = 300,
  max_tries = 3
)
}
\arguments{
\item{url}{Base URL of the docling-serve server. Defaults to the
\code{DOCLING_SERVE_URL} environment variable, falling back to
\code{"http://localhost:5001"}.}

\item{api_key}{API key sent as an \code{X-Api-Key} header. Defaults to the
\code{DOCLING_SERVE_API_KEY} environment variable; \code{NULL} or \code{""} means no
authentication, which is the usual case for a local server.}

\item{timeout}{Request timeout in seconds.}

\item{max_tries}{Number of attempts for transient failures, passed to
\code{\link[httr2:req_retry]{httr2::req_retry()}}.}
}
\value{
An object of class \code{doclr_client}.
}
\description{
Builds a lightweight handle holding the base URL, credentials and request
policy used by every other function in the package. Nothing is sent to the
server until you call \code{\link[=doclr_health]{doclr_health()}} or one of the convert functions.
}
\examples{
client <- doclr_client("http://localhost:5001")
client
}
