% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/status.R
\name{doclr_convert}
\alias{doclr_convert}
\title{Convert a document, waiting for the result}
\usage{
doclr_convert(
  source,
  options = doclr_options(),
  client = doclr_client(),
  poll_interval = 2,
  timeout = 600,
  quiet = !interactive()
)
}
\arguments{
\item{source}{One or more document URLs or local file paths.}

\item{options}{A \code{\link[=doclr_options]{doclr_options()}} object.}

\item{client}{A \code{\link[=doclr_client]{doclr_client()}}.}

\item{poll_interval}{Seconds between status checks.}

\item{timeout}{Give up after this many seconds.}

\item{quiet}{Suppress the progress spinner.}
}
\value{
A \link{doclr_document} object, or a list of them when several sources
were converted.
}
\description{
The function most users want. Submits the source asynchronously, shows a
\code{cli} spinner while the server works, and returns the finished document.
URLs are sent to the source endpoint and everything else is uploaded, so a
mixed vector of URLs and paths is handled in one call.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
doc <- doclr_convert("https://arxiv.org/pdf/2408.09869")
as_markdown(doc)
\dontshow{\}) # examplesIf}
}
