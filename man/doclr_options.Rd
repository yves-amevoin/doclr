% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert.R
\name{doclr_options}
\alias{doclr_options}
\title{Conversion options accepted by docling-serve}
\usage{
doclr_options(
  to_formats = "md",
  from_formats = NULL,
  do_ocr = NULL,
  ocr_lang = NULL,
  do_table_structure = NULL,
  table_mode = NULL,
  image_export_mode = NULL,
  include_images = NULL,
  abort_on_error = NULL,
  ...
)
}
\arguments{
\item{to_formats}{Output formats to produce. Any of \code{"md"}, \code{"json"},
\code{"html"}, \code{"text"}, \code{"doctags"}.}

\item{from_formats}{Input formats the server should accept, e.g. \code{"pdf"},
\code{"docx"}, \code{"html"}. \code{NULL} lets the server decide.}

\item{do_ocr}{Run OCR on scanned content.}

\item{ocr_lang}{Character vector of OCR language codes, e.g. \code{c("eng", "fra")}.}

\item{do_table_structure}{Recover table structure.}

\item{table_mode}{Table model to use, \code{"fast"} or \code{"accurate"}.}

\item{image_export_mode}{How images are returned: \code{"placeholder"},
\code{"embedded"} or \code{"referenced"}.}

\item{include_images}{Include images in the converted output.}

\item{abort_on_error}{Ask the server to fail the whole request when one
source fails.}

\item{...}{Further options passed through verbatim to the API, for fields
this package does not name explicitly.}
}
\value{
A named list of options, class \code{doclr_options}.
}
\description{
Builds the \code{options} object sent with a conversion request. Only the
arguments you set are transmitted, so the server's own defaults apply to
everything else.
}
\examples{
doclr_options(to_formats = c("md", "json"), do_ocr = TRUE)
}
