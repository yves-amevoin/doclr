% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server-management.R
\name{doclr_server_start}
\alias{doclr_server_start}
\title{Start the managed docling-serve container}
\usage{
doclr_server_start(
  port = 5001,
  ui = FALSE,
  engine = NULL,
  name = DOCLR_CONTAINER
)
}
\arguments{
\item{port}{Port to publish the server on.}

\item{ui}{Start the bundled web UI. Off by default to keep the footprint
small.}

\item{engine}{Container engine to use. \code{"auto"} prefers Podman, then
Docker, then Colima if you already have it.}

\item{name}{Container name.}
}
\value{
Invisibly, a \code{\link[=doclr_client]{doclr_client()}} for the running server.
}
\description{
Reuses the container recorded by a previous \code{\link[=doclr_setup]{doclr_setup()}} when there is
one, and creates it otherwise. The chosen engine, container name and port
are written to the user config directory so that
\code{\link[=doclr_server_stop]{doclr_server_stop()}} and \code{\link[=doclr_server_status]{doclr_server_status()}} can find them again.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
doclr_server_start()
\dontshow{\}) # examplesIf}
}
