% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/status.R
\name{doclr_task_status}
\alias{doclr_task_status}
\title{Poll the status of an asynchronous task}
\usage{
doclr_task_status(task, wait = 0, client = doclr_client())
}
\arguments{
\item{task}{A \link{doclr_task} object, or a task id string.}

\item{wait}{Seconds the server may hold the request open before answering.}

\item{client}{A \code{\link[=doclr_client]{doclr_client()}}. Only used when \code{task} is an id string.}
}
\value{
A one-row tibble with \code{task_id}, \code{status} and \code{position}.
}
\description{
Calls \code{GET /v1/status/poll/{task_id}}. With \code{wait} greater than zero the
server holds the connection open until the status changes or the wait
elapses, which is cheaper than tight client-side polling.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
task <- doclr_convert_source_async("https://arxiv.org/pdf/2408.09869")
doclr_task_status(task)
\dontshow{\}) # examplesIf}
}
