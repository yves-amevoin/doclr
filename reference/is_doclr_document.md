# Test whether an object is a converted document

Test whether an object is a converted document

## Usage

``` r
is_doclr_document(x)
```

## Arguments

- x:

  An object.

## Value

`TRUE` if `x` is a `doclr_document`, otherwise `FALSE`.

## Examples

``` r
is_doclr_document(1)
#> [1] FALSE
```
